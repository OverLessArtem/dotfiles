#!/bin/bash

# Copying files
cp -r alacritty ~/.config
cp -r btop ~/.config
cp -r fuzzel ~/.config
cp -r hypr ~/.config
cp -r swaync ~/.config
cp -r waybar ~/.config
cp -r yazi ~/.config


echo "Done!"
